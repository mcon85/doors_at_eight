<%= render 'application/errors' %>
<%= form_for [@venue, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.select :rating, options_for_select(populate_ratings) %>

  <%= f.label :body %>
  <%= f.text_area :body, :rows => 5 %>

  <%= f.submit 'Save Review', class: "button"%>
<% end %>
