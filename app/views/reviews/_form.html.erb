<%= render 'application/errors' %>
<%= form_for [:venue, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit 'Create Rating', class: "button"%>
<% end %>
